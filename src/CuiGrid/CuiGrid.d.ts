import * as React from "react";
import { StandardProps, PropTypes } from "..";

export interface CuiGridProps extends StandardProps<"component"> {}

declare const CuiGrid: React.ComponentType<CuiGridProps>;

export default CuiGrid;
